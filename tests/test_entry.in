extern print(n);
extern printNewLine();

def fib(x)
    if x < 3 then
        1
    else
        fib(x-1) + fib(x-2);

def fibto(x)
    for i = 1, i < x + 1 in
        print(fib(i)) + printNewLine();

def entry()
    fibto(10)
